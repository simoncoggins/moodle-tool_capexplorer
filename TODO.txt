
MENU TODO:
- More refactoring to simplify/reduce duplication

- Add messages when no items or make not expandable?
- Improve error handling
- Preload icons?
- Move into dialog with link to select?
- A-Z sub-folders for large numbers

GENERAL TODO:

Selecting autocomplete via keyboard doesn't hide menu of options.
Improve messages when user checks a capability in an invalid context.
Add unit tests for treelib.php
Remove manually assigned column for auto assigned role if no manual assignments?
Put results on a separate page (with initial results block)?

Add behat tests?
Remove hover styles from userdir node?

